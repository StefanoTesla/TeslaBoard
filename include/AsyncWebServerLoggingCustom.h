#pragma once
#define async_ws_log_e(format, ...) do {} while (0)
#define async_ws_log_w(format, ...) do {} while (0)
#define async_ws_log_i(format, ...) do {} while (0)
#define async_ws_log_d(format, ...) do {} while (0)
#define async_ws_log_v(format, ...) do {} while (0)